import { ccdResourceExplorerPlugin } from './plugin';

describe('ccdResourceExplorerPlugin', () => {
  it('should export plugin', () => {
    expect(ccdResourceExplorerPlugin).toBeDefined();
  });
});
